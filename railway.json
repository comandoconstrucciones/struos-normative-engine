{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r vercel-api/requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn index:app --app-dir vercel-api/api --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}
